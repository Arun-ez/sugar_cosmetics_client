
const initial = {
    data: [],
    sortBy: "",
    filterBy: [],
    isLoading: false,
    isError: false
}

const ProductReducer = (state = initial, action) => {
    switch (action.type) {

        default: {
            return state;
        }
    }
}

export { ProductReducer }